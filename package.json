{
  "name": "apec-france-jobs-scraper",
  "version": "0.0.1",
  "type": "module",
  "description": "Apify actor for apec-france-jobs-scraper",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "apify": "^3.2.0",
    "crawlee": "^3.7.0",
    "playwright": "^1.40.0"
  },
  "scripts": {
    "start": "node src/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}